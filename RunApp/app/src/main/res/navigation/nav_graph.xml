<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/nav_graph"
    app:startDestination="@id/mainActivity">

    <activity
        android:id="@+id/mainActivity"
        android:name="com.example.android.accessibility.runapp.MainActivity"
        android:label="MainActivity" />
    <activity
        android:id="@+id/timerActivity"
        android:name="com.example.android.accessibility.runapp.TimerActivity"
        android:label="activity_timer"
        tools:layout="@layout/activity_timer" />
</navigation>